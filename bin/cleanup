#!/usr/bin/env sh

nix-collect-garbage -d

if [ "$(uname)" = "Darwin" ]; then
  brew cleanup --prune=all -s

  if ! osascript -e 'tell app "Finder" to empty' 2>/dev/null; then
    echo "no items in trash"
  fi
fi
